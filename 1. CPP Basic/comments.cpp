#include<iostream>  // This line includes the iostream library, which allows us to use input and output functions like cout.

using namespace std; // This allows us to use the standard namespace, so we don’t have to prefix standard functions with std::.

int main() {
    // This is a single-line comment.
    // The next line will print "Hello Coders" to the console.
    cout << "Hello Coders" << endl;

    // Another single-line comment:
    // The next line will print "Hello Siva Welcome to Coding".
    cout << "Hello Siva Welcome to Coding" << endl;

    /* 
    This is a multi-line comment.
    Multi-line comments can span several lines.
    They are useful when we need to add detailed explanations
    or temporarily disable a block of code.
    */

    /*
    cout << "This line of code is commented out and won't run!";
    */

    // The program returns 0, indicating that it finished successfully.
    return 0; 
}
